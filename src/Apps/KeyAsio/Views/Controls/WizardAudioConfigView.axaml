<UserControl
  x:Class="KeyAsio.Views.Controls.WizardAudioConfigView"
  xmlns="https://github.com/avaloniaui"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:controls="using:KeyAsio.Views.Controls"
  xmlns:converters="using:KeyAsio.Converters"
  xmlns:lang="clr-namespace:KeyAsio.Lang"
  xmlns:materialIcons="clr-namespace:Material.Icons.Avalonia;assembly=Material.Icons.Avalonia"
  xmlns:suki="https://github.com/kikipoulet/SukiUI"
  xmlns:vm="using:KeyAsio.ViewModels"
  x:DataType="vm:WizardAudioConfigViewModel">
  <UserControl.Styles>
    <Style Selector="suki|GlassCard.promix-option:disabled">
      <Setter Property="Opacity" Value="0.55" />
      <Setter Property="IsInteractive" Value="False" />
    </Style>
  </UserControl.Styles>

  <Grid>
    <Grid
      VerticalAlignment="Center"
      IsVisible="{Binding IsSelectionMode}"
      RowDefinitions="Auto,Auto"
      RowSpacing="20">
      <TextBlock
        Grid.Row="0"
        HorizontalAlignment="Center"
        Classes="h3"
        Text="音频配置向导：选择模式" />
      <Grid
        Grid.Row="1"
        ColumnDefinitions="*,*"
        ColumnSpacing="20">
        <Grid Grid.Column="0">
          <suki:GlassCard
            Classes="promix-option"
            Command="{Binding SelectModeCommand}"
            IsEnabled="{Binding IsProMixAvailable}"
            IsInteractive="True">
            <suki:GlassCard.CommandParameter>
              <vm:WizardMode>Software</vm:WizardMode>
            </suki:GlassCard.CommandParameter>
            <StackPanel Spacing="15">
              <StackPanel
                HorizontalAlignment="Center"
                Orientation="Vertical"
                Spacing="10">
                <materialIcons:MaterialIcon
                  Width="64"
                  Height="64"
                  Foreground="{DynamicResource SukiPrimaryColor}"
                  Kind="AutoFix" />
                <TextBlock
                  FontSize="18"
                  FontWeight="Bold"
                  Text="ProMix 模式"
                  TextAlignment="Center"
                  TextWrapping="Wrap" />
              </StackPanel>

              <StackPanel Height="100" Spacing="15">
                <TextBlock
                  IsVisible="{Binding IsProMixAvailable}"
                  Opacity="0.7"
                  Text="我只有一个声卡（如笔记本内置），或者我不确定。（推荐使用，一键配置）"
                  TextAlignment="Center"
                  TextWrapping="Wrap" />
                <TextBlock
                  FontWeight="DemiBold"
                  Foreground="{DynamicResource SukiDangerColor}"
                  IsVisible="{Binding !IsProMixAvailable}"
                  Text="未检测到 ProMix 插件，软件混音模式当前不可用。"
                  TextAlignment="Center"
                  TextWrapping="Wrap" />
                <Border
                  Padding="10,5"
                  HorizontalAlignment="Center"
                  Background="{DynamicResource SukiPrimaryColor}"
                  CornerRadius="5"
                  IsVisible="{Binding IsProMixAvailable}">
                  <TextBlock
                    FontSize="12"
                    Foreground="White"
                    Text="包含 ProMix 试用权益" />
                </Border>
                <Border
                  Padding="10,5"
                  HorizontalAlignment="Center"
                  Background="{DynamicResource SukiDangerColor}"
                  CornerRadius="5"
                  IsVisible="{Binding !IsProMixAvailable}">
                  <StackPanel Orientation="Horizontal" Spacing="5">
                    <materialIcons:MaterialIcon
                      Width="14"
                      Height="14"
                      Foreground="White"
                      Kind="AlertCircle" />
                    <TextBlock
                      FontSize="12"
                      Foreground="White"
                      Text="插件缺失" />
                  </StackPanel>
                </Border>
              </StackPanel>
            </StackPanel>
          </suki:GlassCard>
        </Grid>

        <Grid Grid.Column="1">
          <suki:GlassCard Command="{Binding SelectModeCommand}" IsInteractive="True">
            <suki:GlassCard.CommandParameter>
              <vm:WizardMode>Hardware</vm:WizardMode>
            </suki:GlassCard.CommandParameter>
            <StackPanel VerticalAlignment="Center" Spacing="15">
              <StackPanel
                HorizontalAlignment="Center"
                Orientation="Vertical"
                Spacing="10">
                <materialIcons:MaterialIcon
                  Width="64"
                  Height="64"
                  Foreground="{DynamicResource SukiPrimaryColor}"
                  Kind="DeveloperBoard" />
                <TextBlock
                  FontSize="18"
                  FontWeight="Bold"
                  Text="手动模式"
                  TextAlignment="Center"
                  TextWrapping="Wrap" />
              </StackPanel>

              <TextBlock
                Height="100"
                Opacity="0.7"
                Text="我有独立声卡，或者我的设备支持 ASIO/WASAPI 独占且不影响系统声音。（需要一定的配置工作）"
                TextAlignment="Center"
                TextWrapping="Wrap" />
            </StackPanel>
          </suki:GlassCard>
        </Grid>
      </Grid>
    </Grid>

    <!--  Hardware Config  -->
    <Grid IsVisible="{Binding IsHardwareConfig}">
      <StackPanel
        Width="600"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Spacing="12">
        <TextBlock
          HorizontalAlignment="Center"
          Classes="h3"
          Text="手动模式向导：选择设备" />

        <Border
          Padding="12"
          Background="{DynamicResource SukiPrimaryColor}"
          CornerRadius="8"
          Opacity="0.85">
          <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10">
            <materialIcons:MaterialIcon
              Width="22"
              Height="22"
              Foreground="White"
              Kind="Information" />
            <StackPanel Grid.Column="1" Spacing="2">
              <TextBlock
                FontWeight="DemiBold"
                Foreground="White"
                Text="请保持 osu! 处于播放音乐状态" />
              <TextBlock
                FontSize="12"
                Foreground="White"
                Opacity="0.85"
                Text="进入选图页，或者首页中保持音乐播放，然后在下方选择可受支持的设备。"
                TextWrapping="Wrap" />
            </StackPanel>
          </Grid>
        </Border>

        <suki:GlassCard Padding="0">
          <StackPanel>
            <Grid
              Margin="16"
              ColumnDefinitions="Auto,*,220"
              ColumnSpacing="12">
              <Panel>
                <Border
                  Width="42"
                  Height="42"
                  Background="{DynamicResource SukiPrimaryColor}"
                  CornerRadius="10"
                  Opacity="0.1" />
                <materialIcons:MaterialIcon
                  Width="22"
                  Height="22"
                  Foreground="{DynamicResource SukiPrimaryColor}"
                  Kind="Tune" />
              </Panel>

              <StackPanel
                Grid.Column="1"
                VerticalAlignment="Center"
                Spacing="2">
                <TextBlock FontWeight="DemiBold" Text="输出类型" />
                <TextBlock
                  FontSize="12"
                  Opacity="0.6"
                  Text="支持原生 ASIO 和 WASAPI " />
              </StackPanel>

              <ComboBox
                Grid.Column="2"
                ItemsSource="{Binding AvailableDriverTypes}"
                SelectedItem="{Binding SelectedDriverType}" />
            </Grid>

            <Separator
              Height="1"
              Margin="16,0"
              Opacity="0.2" />

            <Grid
              Margin="16"
              ColumnDefinitions="Auto,*,270"
              ColumnSpacing="12">
              <Panel>
                <Border
                  Width="42"
                  Height="42"
                  Background="{DynamicResource SukiPrimaryColor}"
                  CornerRadius="10"
                  Opacity="0.1" />
                <materialIcons:MaterialIcon
                  Width="22"
                  Height="22"
                  Foreground="{DynamicResource SukiPrimaryColor}"
                  Kind="Speaker" />
              </Panel>

              <StackPanel
                Grid.Column="1"
                VerticalAlignment="Center"
                Spacing="2">
                <TextBlock FontWeight="DemiBold" Text="KeyASIO 播放设备" />
                <TextBlock
                  FontSize="12"
                  Opacity="0.6"
                  Text="WASAPI 设备会以独占模式创建" />
              </StackPanel>

              <ComboBox
                Grid.Column="2"
                ItemsSource="{Binding AvailableAudioDevices}"
                SelectedItem="{Binding SelectedAudioDevice}">
                <ComboBox.ItemTemplate>
                  <DataTemplate>
                    <TextBlock Text="{Binding FriendlyName}" />
                  </DataTemplate>
                </ComboBox.ItemTemplate>
              </ComboBox>
            </Grid>

            <Border
              Margin="16,0,16,16"
              Padding="10"
              Background="{DynamicResource SukiDangerColor}"
              CornerRadius="6"
              IsVisible="{Binding ShowHardwareDriverWarning}"
              Opacity="0.85">
              <TextBlock
                FontSize="12"
                Foreground="White"
                Text="{Binding HardwareDriverWarning}"
                TextWrapping="Wrap" />
            </Border>
          </StackPanel>
        </suki:GlassCard>

        <TextBlock
          HorizontalAlignment="Center"
          FontSize="12"
          Opacity="0.65"
          Text="点击“创建设备并开始试听”后，将每秒播放一次测试军鼓。"
          TextWrapping="Wrap" />
      </StackPanel>
    </Grid>

    <!--  Hardware concurrency check  -->
    <Grid IsVisible="{Binding IsConcurrencyCheck}">
      <StackPanel
        Width="600"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Spacing="14">
        <TextBlock
          HorizontalAlignment="Center"
          Classes="h3"
          Text="手动模式向导：测试设备" />

        <suki:GlassCard Padding="18">
          <StackPanel Spacing="14">
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
              <Panel>
                <Border
                  Width="44"
                  Height="44"
                  Background="{DynamicResource SukiPrimaryColor}"
                  CornerRadius="22"
                  Opacity="0.15" />
                <materialIcons:MaterialIcon
                  Width="24"
                  Height="24"
                  Foreground="{DynamicResource SukiPrimaryColor}"
                  Kind="VolumeHigh" />
              </Panel>
              <StackPanel
                Grid.Column="1"
                VerticalAlignment="Center"
                Spacing="2">
                <TextBlock FontWeight="DemiBold" Text="测试军鼓播放中" />
                <TextBlock
                  FontSize="12"
                  Opacity="0.65"
                  Text="KeyASIO 已独占所选设备" />
              </StackPanel>
            </Grid>

            <Separator Opacity="0.2" />

            <TextBlock Text="在 osu! 的音频设置中，选择这块声卡对应的设备：" TextWrapping="Wrap" />
            <Border
              Padding="10"
              Background="{DynamicResource SukiControlBorderBrush}"
              CornerRadius="6"
              Opacity="0.75">
              <TextBlock
                HorizontalAlignment="Center"
                FontWeight="DemiBold"
                Text="{Binding SelectedAudioDevice.FriendlyName, Converter={x:Static converters:AsioToAudioConverter.Instance}}"
                TextTrimming="CharacterEllipsis" />
            </Border>
            <TextBlock
              HorizontalAlignment="Center"
              FontSize="16"
              FontWeight="DemiBold"
              Text="osu! 的音乐还能听到吗？" />
          </StackPanel>
        </suki:GlassCard>

        <StackPanel
          HorizontalAlignment="Center"
          Orientation="Horizontal"
          Spacing="10">
          <Button
            Classes="Flat"
            Command="{Binding ReportSameDeviceSilentCommand}"
            Content="听不到" />
          <Button
            Classes="Flat Primary"
            Command="{Binding ConfirmSameDeviceAudioCommand}"
            Content="听得到" />
        </StackPanel>
      </StackPanel>
    </Grid>

    <!--  Alternate game device check  -->
    <Grid IsVisible="{Binding IsAlternativeDeviceCheck}">
      <StackPanel
        Width="600"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Spacing="14">
        <TextBlock
          HorizontalAlignment="Center"
          Classes="h3"
          Text="为 osu! 选择其他输出设备" />

        <suki:GlassCard Padding="18">
          <StackPanel Spacing="12">
            <TextBlock
              FontWeight="DemiBold"
              Text="当前设备不支持“多会话独占”，但 osu! 仍可使用其他设备播放。"
              TextWrapping="Wrap" />
            <TextBlock
              Opacity="0.72"
              Text="请在 osu! 中尝试其他设备，直到找到能正常播放音乐的设备后确认。通常情况下，osu! 会在其他设备中输出播放（例如显示器、USB音响等）。"
              TextWrapping="Wrap" />
            <Border
              Padding="10"
              Background="{DynamicResource SukiPrimaryColor}"
              CornerRadius="6"
              Opacity="0.85">
              <StackPanel Orientation="Horizontal" Spacing="8">
                <materialIcons:MaterialIcon
                  Width="18"
                  Height="18"
                  Foreground="White"
                  Kind="VolumeHigh" />
                <TextBlock Foreground="White" Text="测试军鼓播放中，可继续逐个尝试" />
              </StackPanel>
            </Border>
          </StackPanel>
        </suki:GlassCard>

        <StackPanel
          HorizontalAlignment="Center"
          Orientation="Horizontal"
          Spacing="10">
          <Button
            Classes="Flat Primary"
            Command="{Binding ConfirmAlternativeDeviceAudioCommand}"
            Content="尝试 ProMix，一键配置" />
          <Button
            Classes="Flat"
            Command="{Binding ReportNoAlternativeDeviceCommand}"
            Content="全部试过，都没声音" />
          <Button
            Classes="Flat Primary"
            Command="{Binding ConfirmAlternativeDeviceAudioCommand}"
            Content="找到有声音的设备" />
        </StackPanel>
      </StackPanel>
    </Grid>

    <!--  ProMix required  -->
    <Grid IsVisible="{Binding IsProMixRequired}">
      <StackPanel
        Width="560"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Spacing="16">
        <materialIcons:MaterialIcon
          Width="56"
          Height="56"
          HorizontalAlignment="Center"
          Foreground="{DynamicResource SukiDangerColor}"
          Kind="AlertCircle" />
        <TextBlock
          HorizontalAlignment="Center"
          Classes="h3"
          Text="建议使用 ProMix 模式" />
        <TextBlock
          HorizontalAlignment="Center"
          Opacity="0.75"
          Text="{Binding ProMixRequiredMessage}"
          TextAlignment="Center"
          TextWrapping="Wrap" />
        <TextBlock
          HorizontalAlignment="Center"
          Foreground="{DynamicResource SukiDangerColor}"
          IsVisible="{Binding !IsProMixAvailable}"
          Text="当前未加载 ProMix 插件，请安装插件后重新运行向导。"
          TextAlignment="Center"
          TextWrapping="Wrap" />
        <StackPanel
          HorizontalAlignment="Center"
          Orientation="Horizontal"
          Spacing="10">
          <Button
            Classes="Flat"
            Command="{Binding RetryHardwareSetupCommand}"
            Content="重新选择独占设备" />
          <Button
            Classes="Flat Primary"
            Command="{Binding SwitchToProMixCommand}"
            Content="切换到 ProMix 模式"
            IsEnabled="{Binding IsProMixAvailable}" />
        </StackPanel>
      </StackPanel>
    </Grid>

    <!--  Software Config  -->
    <Grid IsVisible="{Binding IsSoftwareConfig}">
      <StackPanel
        Width="550"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Spacing="20">
        <TextBlock
          HorizontalAlignment="Center"
          Classes="h3"
          Text="软件 / ProMix 模式配置" />

        <suki:GlassCard Padding="0">
          <StackPanel>
            <!--  Virtual Driver Section  -->
            <Grid Margin="20" ColumnDefinitions="Auto,*,Auto">
              <Panel>
                <!--  Warning State  -->
                <Panel IsVisible="{Binding ShowVirtualDriverWarning}">
                  <Border
                    Width="48"
                    Height="48"
                    Background="Orange"
                    CornerRadius="12"
                    Opacity="0.1" />
                  <materialIcons:MaterialIcon
                    Width="24"
                    Height="24"
                    Foreground="Orange"
                    Kind="AlertCircle" />
                </Panel>
                <!--  Success State  -->
                <Panel IsVisible="{Binding !ShowVirtualDriverWarning}">
                  <Border
                    Width="48"
                    Height="48"
                    Background="{DynamicResource SukiSuccessColor}"
                    CornerRadius="12"
                    Opacity="0.1" />
                  <materialIcons:MaterialIcon
                    Width="24"
                    Height="24"
                    Foreground="{DynamicResource SukiSuccessColor}"
                    Kind="CheckCircle" />
                </Panel>
              </Panel>

              <StackPanel
                Grid.Column="1"
                Margin="15,0"
                VerticalAlignment="Center"
                Spacing="4">
                <TextBlock FontWeight="DemiBold" Text="虚拟驱动状态" />
                <TextBlock
                  FontSize="13"
                  Opacity="0.7"
                  Text="{Binding VirtualDriverWarning, TargetNullValue={I18N {x:Static lang:SRKeys.Wizard_VirtualDriver_Detected}}}"
                  TextTrimming="CharacterEllipsis" />
              </StackPanel>

              <StackPanel
                Grid.Column="2"
                IsVisible="{Binding ShowVirtualDriverWarning}"
                Orientation="Horizontal"
                Spacing="10">
                <Button
                  VerticalAlignment="Center"
                  Classes="Flat"
                  Command="{Binding DownloadVirtualDriverCommand}"
                  Content="下载"
                  ToolTip.Tip="下载 VB-Audio Cable" />
                <Button
                  VerticalAlignment="Center"
                  Classes="Flat"
                  Command="{Binding RetryVirtualDriverCheckCommand}"
                  Content="刷新" />
              </StackPanel>
            </Grid>

            <Separator
              Height="1"
              Margin="20,0"
              Background="{DynamicResource SukiControlBorderBrush}"
              Opacity="0.2" />

            <!--  Device Selection  -->
            <Grid Margin="20" ColumnDefinitions="Auto,*,Auto">
              <Panel>
                <Border
                  Width="48"
                  Height="48"
                  Background="{DynamicResource SukiPrimaryColor}"
                  CornerRadius="12"
                  Opacity="0.1" />
                <materialIcons:MaterialIcon
                  Width="24"
                  Height="24"
                  Foreground="{DynamicResource SukiPrimaryColor}"
                  Kind="Headphones" />
              </Panel>

              <StackPanel
                Grid.Column="1"
                Margin="15,0"
                VerticalAlignment="Center"
                Spacing="4">
                <TextBlock FontWeight="DemiBold" Text="物理输出设备" />
                <TextBlock
                  FontSize="12"
                  Opacity="0.6"
                  Text="选择耳机或音响" />
              </StackPanel>

              <ComboBox
                Grid.Column="2"
                MinWidth="200"
                ItemsSource="{Binding AvailableAudioDevices}"
                SelectedItem="{Binding SelectedAudioDevice}">
                <ComboBox.ItemTemplate>
                  <DataTemplate>
                    <TextBlock Text="{Binding FriendlyName}" />
                  </DataTemplate>
                </ComboBox.ItemTemplate>
              </ComboBox>
            </Grid>

            <Separator
              Height="1"
              Margin="20,0"
              Background="{DynamicResource SukiControlBorderBrush}"
              Opacity="0.2" />

            <!--  Notice  -->
            <Grid Margin="20" ColumnDefinitions="Auto,*">
              <Panel>
                <Border
                  Width="48"
                  Height="48"
                  Background="{DynamicResource SukiPrimaryColor}"
                  CornerRadius="12"
                  Opacity="0.1" />
                <materialIcons:MaterialIcon
                  Width="24"
                  Height="24"
                  Foreground="{DynamicResource SukiPrimaryColor}"
                  Kind="GamepadVariant" />
              </Panel>

              <StackPanel
                Grid.Column="1"
                Margin="15,0"
                VerticalAlignment="Center"
                Spacing="4">
                <TextBlock FontWeight="DemiBold" Text="游戏设置指引" />
                <TextBlock
                  FontSize="12"
                  Opacity="0.6"
                  Text="请在 osu! 设置中，将输出设备修改为 [VB-Audio Cable]"
                  TextWrapping="Wrap" />
              </StackPanel>
            </Grid>
          </StackPanel>
        </suki:GlassCard>
      </StackPanel>
    </Grid>

    <!--  Validation  -->
    <Grid IsVisible="{Binding IsValidationStep}">
      <StackPanel
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Spacing="20">
        <TextBlock
          HorizontalAlignment="Center"
          Classes="h3"
          Text="{Binding ValidationMessage}" />

        <suki:Loading IsVisible="{Binding IsValidationRunning}" />

        <materialIcons:MaterialIcon
          Width="60"
          Height="60"
          Foreground="{DynamicResource SukiSuccessColor}"
          IsVisible="{Binding ValidationSuccess}"
          Kind="CheckCircle" />

        <StackPanel IsVisible="{Binding !IsValidationRunning}">
          <StackPanel IsVisible="{Binding !ValidationSuccess}" Spacing="10">
            <materialIcons:MaterialIcon
              Width="60"
              Height="60"
              Foreground="{DynamicResource SukiDangerColor}"
              Kind="CloseCircle" />
            <TextBlock
              HorizontalAlignment="Center"
              Opacity="0.7"
              Text="当前设备不受支持，请返回并重新选择其他设备。"
              TextAlignment="Center"
              TextWrapping="Wrap" />
          </StackPanel>

          <StackPanel IsVisible="{Binding ValidationSuccess}" Spacing="10">
            <TextBlock
              HorizontalAlignment="Center"
              Opacity="0.7"
              Text="{Binding ValidationInstruction}"
              TextAlignment="Center"
              TextWrapping="Wrap" />
          </StackPanel>
        </StackPanel>
      </StackPanel>
    </Grid>
  </Grid>
</UserControl>
